import { PersonProperties } from "../person.entity";
import { PersonFakeBuilder } from "../person-fake-builder";

describe("PersonFakeBuilder", () => {
  it("should create a person fake builder", () => {
    // const faker: PersonProperties = PersonFakeBuilder.create();

    // expect(faker).toBeDefined();
    // expect(faker.userId).toBeDefined();
    // expect(faker.gender).toBeDefined();
    // expect(faker.location).toBeDefined();
    // expect(faker.birthdate).toBeInstanceOf(Date);
    // expect(faker.biography).toBeDefined();
    // expect(faker.shareableSection).toBeDefined();
    // expect(faker.isOpenToWork).toBeDefined();
    // expect(faker.isFreelancer).toBeDefined();
    // expect(faker.image).toBeDefined();
    // expect(faker.createdAt).toBeInstanceOf(Date);
    // expect(faker.updatedAt).toBeInstanceOf(Date);
  });

  it("should create unique userIds", () => {
    // const faker1: PersonProperties = PersonFakeBuilder.create();
    // const faker2: PersonProperties = PersonFakeBuilder.create();

    // expect(faker1.userId).not.toStrictEqual(faker2.userId);
  });
});
